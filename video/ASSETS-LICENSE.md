# Audio assets

The ambient bed and both sound accents are synthesized by build.py (sine/pink-noise sources through ffmpeg filters) at build time. No third-party audio is used anywhere in the video; no licence is required.
