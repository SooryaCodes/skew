"use client";

/**
 * Retired by the design reset: the film grain and 1px grid belonged to the
 * brass-on-graphite identity. The new system gets its depth from layered
 * neutrals and whitespace, not texture. Kept as a no-op so call sites and
 * history stay intact.
 */

export function Texture() {
  return null;
}
