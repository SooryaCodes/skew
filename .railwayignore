# Keep deploy tarballs small. BOTH services upload from the repo root:
# api builds backend/Dockerfile, web builds with rootDirectory=frontend —
# so frontend/ must NOT be excluded here, only its heavy artifacts.
video/
docs/
.git/
backend/.venv/
frontend/node_modules/
frontend/.next/
**/__pycache__/
**/*.mp4
**/*.webm
**/*.wav
